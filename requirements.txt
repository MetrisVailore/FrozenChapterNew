# Core ML dependencies
torch>=2.1.1
transformers>=4.36.0
accelerate>=0.25.0
peft>=0.7.0
trl>=0.7.0
bitsandbytes>=0.41.0

# Data processing
datasets>=2.15.0
jsonlines>=4.0.0
pandas>=2.0.0
numpy>=1.24.0

# Monitoring and logging
wandb>=0.16.0
tensorboard>=2.15.0

# Utilities
tqdm>=4.66.0
scikit-learn>=1.3.0

# Optional: Flash Attention (highly recommended for 2-4x speedup)
# Install separately: pip install flash-attn --no-build-isolation
# flash-attn>=2.3.0
ujson>=5.6.0